allprojects {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

subprojects {
    version = '1.0'

    apply plugin: 'java'

    dependencies {
        testCompile "junit:junit:$junit_version"
    }
}